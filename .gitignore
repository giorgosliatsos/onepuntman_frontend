# dependencies
/node_modules

# Vite build output
/dist

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (VITE_* vars aren't secret since they're bundled client-side,
# but per-environment .env files still shouldn't be committed)
.env
.env.local
.env.*.local

# IDE
.vscode/*
!.vscode/extensions.json

# typescript
*.tsbuildinfo
